(function($){
	$(document).ready(function($) {
		// Animations ou interactions pour le bloc intro-accueil

		// Exemple : Animation au scroll
		$('.intro-accueil').each(function() {
			const $block = $(this);

			// Ajouter des animations ou interactions ici si nécessaire
		});
	});

	$(window).load(function() {
		// Code à exécuter après le chargement complet de la page
	});
})(jQuery);
