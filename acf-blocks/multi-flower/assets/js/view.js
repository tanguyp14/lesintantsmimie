(function($){
	$(document).ready(function($) {
		// Animations ou interactions pour le bloc multi-flower

		// Exemple : Animation au scroll pour les fleurs
		$('.multi-flower__item').each(function() {
			const $flower = $(this);

			// Ajouter des animations parallax ou au scroll si nécessaire
		});
	});

	$(window).load(function() {
		// Code à exécuter après le chargement complet de la page
	});
})(jQuery);
